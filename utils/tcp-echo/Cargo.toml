[package]
name = "tcp-echo"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "TCP echo server+client for testing tcpstats-reader socket stats collection"

[dependencies]
anyhow = "1"
thiserror = "2"
libc = "0.2"
