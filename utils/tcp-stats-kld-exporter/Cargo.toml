[package]
name = "tcp-stats-kld-exporter"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "Prometheus exporter for bsd-xtcp tcp_stats_kld kernel module statistics"

[dependencies]
bsd-xtcp = { path = "../.." }
tiny_http = "0.12"
anyhow = "1"
